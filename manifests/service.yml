apiVersion: v1
kind: Service
metadata:
    name: "jzmollusc"
    labels:
        app: "jzmollusc"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
    selector:
        app: "jzmollusc"