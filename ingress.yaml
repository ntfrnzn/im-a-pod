apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: cloudflare-warp
  name: im-a-pod
spec:
  rules:
  - host: im-a-pod.anthopleura.net
    http:
      paths:
      - backend:
          serviceName: im-a-pod
          servicePort: 80
        path: /
status:
  loadBalancer: {}
